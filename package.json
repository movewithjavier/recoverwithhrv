{
  "name": "recoverwithhrv",
  "version": "1.0.0",
  "description": "Recover with HRV Website",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "prepare-replit": "node scripts/prepare-for-replit.js",
    "migrate": "node scripts/migrate-from-dual-repos.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "stripe": "^14.25.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
